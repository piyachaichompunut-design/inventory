name: Daily Morning Report
on:
  schedule:
    - cron: '45 0 * * *'   # 07:45 น. ไทย (00:45 UTC)
  workflow_dispatch:

jobs:
  morning-report:
    runs-on: ubuntu-latest
    steps:
      - name: Call /api/cron
        run: |
          curl -s "https://inventory-rho-hazel.vercel.app/api/cron"
